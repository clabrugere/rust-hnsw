[package]
name = "rust-hnsw"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.9.2", features = [ "small_rng" ] }

[dev-dependencies]
criterion = { version = "0.7", features = ["html_reports"]}

[[bench]]
name = "bench"
harness = false
